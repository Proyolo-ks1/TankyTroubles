import { getGlobal } from './global-state.js';

export function generateMaze() {
    const maze = 192 // is currently just the tilesize, not a whole maze object
    // getGlobal().tileSize = maze;
    return maze
}